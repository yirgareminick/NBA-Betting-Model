# Path configuration for NBA Betting Model

# Data directories
data:
  root: "data"
  raw: "data/raw"
  processed: "data/processed"
  predictions: "data/predictions"
  
# Model directories
models:
  root: "models"
  trained: "models"
  
# Output directories
outputs:
  logs: "logs"
  reports: "reports"
  performance: "reports/performance"
  
# Notebooks
notebooks:
  root: "notebooks"
  
# Source code
src:
  root: "src"
  features: "src/features"
  models: "src/models"
  predict: "src/predict"
  ingest: "src/ingest"
  stake: "src/stake"
  validation: "src/validation"
  
# Configuration
configs:
  root: "configs"